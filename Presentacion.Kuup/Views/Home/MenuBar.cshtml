<header>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand" href="@Url.Action("Index","Home")"><i class="fa fa-home"></i> Home</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" data-controls="navbarColor01" aria-expanded="false">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                @Html.Raw(ViewData["lstMenu"])
            </ul>
            <span class="navbar-text">
                <a href="@Url.Action("LoginOut", "Account")"><i class="fa fa-sign-out"></i> Cerrar sesión</a>
            </span>
        </div>
    </nav>
</header>
